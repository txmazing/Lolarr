# Lolarr Mobile

Placeholder for the future mobile target.

Recommended direction: Capacitor wrapping the `apps/web` build output, with native platform folders added only when the mobile shell is needed.
